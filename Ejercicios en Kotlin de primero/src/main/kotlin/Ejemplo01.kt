fun main() {

    // meter datos por teclado
    val nombre = readln().toString() // así leemos una cadena (al poner toString()) habiendole metido un valor por teclado
    //  val edad = readln().toInt() // así leemos un entero habiendole metido un valor por teclado
    //  val altura = readln().toDouble() // así leemos un double habiendole metido un valor por teclado

    println("hola, $nombre")


}

